---
// PIN-PROTECTED — see middleware.ts (PIN: 060622)
import "../styles/globals.css";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Website — Spirit Media Publishing</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;600;700&display=swap" rel="stylesheet" />
  </head>

  <body class="bg-stone-50 text-stone-800">
    <div class="min-h-screen flex flex-col">

      <header class="border-b border-stone-200 bg-white">
        <div class="max-w-2xl mx-auto px-6 py-6">
          <img src="https://cdn.sanity.io/images/pmowd8uo/production/dc16e3c604bd5d52de7647af46836ff418c22898-6575x656.png?w=440&h=88&fit=max&auto=format" alt="Spirit Media Publishing" class="h-10" />
        </div>
      </header>

      <main class="flex-1 max-w-2xl mx-auto px-6 py-12 w-full space-y-10">

        <!-- Update Your Site -->
        <section>
          <h1 class="text-3xl font-bold mb-1">Update Your Site</h1>
          <p id="site-label" class="text-stone-500 text-sm mb-6"></p>

          <!-- No-slug fallback -->
          <div id="no-site" class="hidden p-6 bg-stone-100 rounded-lg text-center text-stone-500 text-sm">
            Contact <a href="mailto:hello@spiritmediapublishing.com" class="text-red-700 font-semibold hover:underline">hello@spiritmediapublishing.com</a> to get your personalized link.
          </div>

          <!-- Update form -->
          <div id="update-section" class="hidden">
            <form id="update-form" class="space-y-4">
              <div>
                <label for="request-text" class="block text-sm font-semibold text-stone-700 mb-2">What would you like to change?</label>
                <textarea id="request-text" required rows="5"
                  placeholder="e.g. Change the hero headline to "Walking in Freedom" and update the button to say "Start Your Journey""
                  class="w-full border border-stone-300 rounded-lg px-4 py-3 text-stone-800 placeholder-stone-400 focus:outline-none focus:ring-2 focus:ring-stone-400 resize-y text-sm"></textarea>
                <p class="text-xs text-stone-400 mt-1">Be specific. The more detail you give, the better the result.</p>
              </div>
              <button type="submit" id="submit-btn"
                class="w-full bg-red-700 hover:bg-red-800 text-white font-semibold py-3 rounded-lg transition-colors disabled:opacity-50 disabled:cursor-not-allowed">
                Submit Request
              </button>
            </form>

            <div id="status-area" class="hidden mt-5">
              <div id="spinner" class="hidden flex items-center gap-3 text-stone-500 text-sm">
                <svg class="animate-spin h-4 w-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                  <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                  <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8z"></path>
                </svg>
                <span id="status-text">Sending to Claude…</span>
              </div>
              <div id="result" class="hidden p-5 bg-green-50 border border-green-200 rounded-lg">
                <p class="font-semibold text-green-800 text-sm">Request received!</p>
                <p class="text-green-700 text-sm mt-1">The SMP team has been notified and will review before anything goes live.</p>
              </div>
              <div id="ambiguous" class="hidden p-5 bg-amber-50 border border-amber-200 rounded-lg">
                <p class="font-semibold text-amber-800 text-sm mb-1">We need more detail</p>
                <p id="ambiguous-text" class="text-amber-700 text-sm"></p>
              </div>
              <div id="error-box" class="hidden p-4 bg-red-50 border border-red-200 rounded-lg">
                <p class="font-semibold text-red-800 text-sm mb-1">Something went wrong</p>
                <p id="error-text" class="text-red-700 text-sm"></p>
              </div>
            </div>

            <p class="text-xs text-stone-400 mt-4">Every change is reviewed by SMP before it goes live.</p>
          </div>
        </section>

        <hr class="border-stone-200" />

        <!-- Sanity Studio -->
        <section>
          <h2 class="text-2xl font-bold mb-1">Edit Content Directly</h2>
          <p class="text-stone-500 text-sm mb-4">For bios, FAQs, team members, images, and settings — use your Sanity Studio. Changes go live in ~2 minutes after you click Publish.</p>
          <div class="bg-stone-100 rounded-lg p-4 text-sm text-stone-600 space-y-1">
            <p>1. Open your Sanity Studio (link provided at launch)</p>
            <p>2. Find and edit the content</p>
            <p>3. Click <strong>Publish</strong></p>
          </div>
        </section>

        <hr class="border-stone-200" />

        <!-- Contact SMP -->
        <section>
          <h2 class="text-2xl font-bold mb-1">Need Something Bigger?</h2>
          <p class="text-stone-500 text-sm mb-4">New pages, layout changes, forms, or anything structural — contact us and we'll handle it.</p>
          <a href="mailto:hello@spiritmediapublishing.com" class="inline-block px-5 py-2.5 bg-stone-800 hover:bg-stone-700 text-white font-semibold rounded-lg text-sm transition-colors">
            hello@spiritmediapublishing.com
          </a>
        </section>

      </main>

      <footer class="border-t border-stone-200 mt-12">
        <div class="max-w-2xl mx-auto px-6 py-6 text-center text-stone-400 text-xs">
          Spirit Media Publishing &copy; 2026
        </div>
      </footer>
    </div>

    <script>
      const params   = new URLSearchParams(window.location.search);
      const siteSlug = params.get('site') || '';

      async function init() {
        if (!siteSlug) {
          document.getElementById('no-site')!.classList.remove('hidden');
          return;
        }
        try {
          const resp  = await fetch('/api/tools-site-update');
          const sites: { slug: string; title: string }[] = await resp.json();
          const match = sites.find(s => s.slug === siteSlug);
          if (!match) { document.getElementById('no-site')!.classList.remove('hidden'); return; }
          document.getElementById('site-label')!.textContent = match.title;
          document.getElementById('update-section')!.classList.remove('hidden');
        } catch {
          document.getElementById('no-site')!.classList.remove('hidden');
        }
      }

      init();

      document.getElementById('update-form')!.addEventListener('submit', async (e) => {
        e.preventDefault();
        const btn         = document.getElementById('submit-btn') as HTMLButtonElement;
        const requestText = (document.getElementById('request-text') as HTMLTextAreaElement).value.trim();
        btn.disabled = true; btn.textContent = 'Submitting…';
        document.getElementById('status-area')!.classList.remove('hidden');
        document.getElementById('spinner')!.classList.remove('hidden');
        ['result', 'ambiguous', 'error-box'].forEach(id => document.getElementById(id)!.classList.add('hidden'));

        const messages = ['Sending to Claude…', 'Reviewing your site…', 'Making edits…', 'Almost done…'];
        let i = 0;
        const statusText = document.getElementById('status-text')!;
        statusText.textContent = messages[0];
        const interval = setInterval(() => { i = Math.min(i + 1, messages.length - 1); statusText.textContent = messages[i]; }, 20000);

        try {
          const resp  = await fetch('/api/tools-site-update', {
            method: 'POST', headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ site_slug: siteSlug, request_text: requestText }),
          });
          const start = await resp.json();
          if (start.error) throw new Error(start.error);

          const poll = async (): Promise<void> => {
            await new Promise(r => setTimeout(r, 5000));
            const job = await (await fetch(`/api/tools-site-update-status?job_id=${start.job_id}`)).json();
            if (job.status === 'pending') return poll();
            clearInterval(interval);
            document.getElementById('spinner')!.classList.add('hidden');
            if (job.status === 'ambiguous') {
              document.getElementById('ambiguous')!.classList.remove('hidden');
              document.getElementById('ambiguous-text')!.textContent = job.message || 'Please add more detail and try again.';
            } else if (job.status === 'failed') {
              document.getElementById('error-box')!.classList.remove('hidden');
              document.getElementById('error-text')!.textContent = job.error || 'Something went wrong. Please try again or contact SMP.';
            } else {
              document.getElementById('result')!.classList.remove('hidden');
              (document.getElementById('update-form') as HTMLFormElement).reset();
            }
            btn.disabled = false; btn.textContent = 'Submit Request';
          };
          poll();
        } catch (err: any) {
          clearInterval(interval);
          document.getElementById('spinner')!.classList.add('hidden');
          document.getElementById('error-box')!.classList.remove('hidden');
          document.getElementById('error-text')!.textContent = err.message;
          btn.disabled = false; btn.textContent = 'Submit Request';
        }
      });
    </script>
  </body>
</html>
